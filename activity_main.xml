<?xml version="1.0" encoding="utf-8"?>
<!-- activity_main.xml -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Top banner ad placeholder (seamlessly integrated at top) -->
    <!-- Replace with actual AdView in your activity code / layout when integrating AdMob.
         Keep this view narrow so it doesn't dominate the white, light-blue theme. -->
    <FrameLayout
        android:id="@+id/top_ad_container"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="0dp"
        android:foregroundGravity="center"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Example AdView (uncomment & use real AdView element when integrating SDK)
        <com.google.android.gms.ads.AdView
            android:id="@+id/adViewTop"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            ads:adSize="BANNER"
            ads:adUnitId="@string/admob_banner_id_top" />
        -->

        <!-- Simple visual placeholder for design / preview -->
        <TextView
            android:id="@+id/top_ad_placeholder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="Top Banner Ad"
            android:textColor="#90CAF9"
            android:textStyle="bold"
            android:padding="8dp" />
    </FrameLayout>

    <!-- Main content area where fragments/pages will be swapped -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginBottom="72dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph"
        android:layout_marginTop="56dp"
        android:background="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/top_ad_container"
        app:layout_constraintBottom_toTopOf="@id/bottom_ad_container" />

    <!-- Bottom banner ad placeholder (above bottom navigation) -->
    <FrameLayout
        android:id="@+id/bottom_ad_container"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_gravity="bottom"
        android:layout_marginBottom="56dp">

        <!-- Example AdView spot for bottom-of-content ad -->
        <!-- <com.google.android.gms.ads.AdView
            android:id="@+id/adViewBottom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            ads:adSize="BANNER"
            ads:adUnitId="@string/admob_banner_id_bottom" /> -->

        <TextView
            android:id="@+id/bottom_ad_placeholder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="Bottom Banner Ad"
            android:textColor="#90CAF9"
            android:textStyle="bold"
            android:padding="8dp" />
    </FrameLayout>

    <!-- Bottom navigation (three buttons: Exchange Rates (Cx), Gold, Calculate) -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_gravity="bottom"
        android:background="#FFFFFF"
        app:itemIconTint="@color/bottom_nav_icon_tint"
        app:itemTextColor="@color/bottom_nav_text_tint"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu"
        android:elevation="8dp" />

    <!-- Floating area for interstitial ad or full-screen ad triggers (hidden by default).
         Use this container to present interstitial-like content on natural transitions. -->
    <FrameLayout
        android:id="@+id/interstitial_ad_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:background="#80000000" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
